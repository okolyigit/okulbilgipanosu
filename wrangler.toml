name = "okul-panosu"
compatibility_date = "2025-01-01"
pages_build_output_dir = "dist"

[[d1_databases]]
binding = "DB"
database_name = "okul-panosu"
database_id = "REPLACE_WITH_REAL_ID_AFTER_CREATE"

# JWT_SECRET production'da `wrangler pages secret put JWT_SECRET` ile set edilecek.
# Lokal geliştirme için .dev.vars dosyasında JWT_SECRET=... yazın.
